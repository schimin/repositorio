// QUANDO FOR CHAVE
for (const [key, value] of Object.entries(cesta)) {
  if (key.includes('quantidade')) {
    // 'quantidade' é uma chave dentro do array
  }
}


// QUANDO FOR VALOR
const lista = [{}]; // sua lista 
const produto  = lista.find(item => item.id == 6)

(produto) ? produto : 'Nada encontrado';
